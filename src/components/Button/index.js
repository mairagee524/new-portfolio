import React from 'react';
import './style.css';

const STYLES = [
    'btn-primary',
    'btn-outline'
]

const SIZES = [
    'btn-md',
    'btn-lg'
]

export const Button = ({
    children,
    type, 
    onClick,
    buttonStyle,
    buttonSize
}) => {
    
}